{
  "name": "mtfh-tl-e2e-tests",
  "version": "1.0.0",
  "description": "End to end tests for MTFH T&L",
  "main": "index.js",
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:cypress:open": "cypress open",
    "test:cypress:run": "cypress run -e TAGS='not @GoogleLighthouse and not @Accessibility and not @ignore and not @flaky and not @device and not @LongRunning and not @ignoreDev'",
    "test:cypress:development": "cypress run --browser chrome --headless -e TAGS='not @GoogleLighthouse and not @Accessibility and not @ignore and not @flaky and not @device and not @LongRunning and not @ignoreDev'",
    "test:cypress:staging": "cypress run --browser chrome --headless -e TAGS='not @GoogleLighthouse and not @Accessibility and not @ignore and not @flaky and not @device and not @LongRunning and not @ignoreStaging'",
    "test:cypress:production": "cypress --browser chrome --headless run -e TAGS='@Production and not @ignore and not @ignoreProd'",
    "test:cypress:accessibility": "cypress run -e TAGS='@Accessibility'",
    "test:cypress:GoogleLighthouse": "cypress run -e TAGS='@GoogleLighthouse'",
    "test:cypress:staging:devices": "cypress run --browser chrome --headless -e TAGS='@device and not @ignore and not @ignoreStaging'",
    "test:cypress:api": "cypress run -e TAGS='@api'",
    "test:cypress:smoke": "cypress run --browser chrome --headless -e TAGS='@SmokeTest and not @ignore'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@testing-library/cypress": "^7.0.6",
    "axe-core": "^4.2.1",
    "browserslist": ">=4.16.5",
    "cypress": "^8.3.1",
    "cypress-audit": "^0.3.0",
    "cypress-axe": "^0.12.2",
    "cypress-cucumber-preprocessor": "^4.2.0",
    "cypress-localstorage-commands": "^1.4.4",
    "cypress-social-logins": "^1.11.3",
    "glob-parent": ">=5.1.2",
    "ws": ">=6.2.2"
  },
  "dependencies": {
    "axios": "^0.21.3",
    "date-and-time": "^1.0.1",
    "uuid": "^8.3.2"
  }
}
