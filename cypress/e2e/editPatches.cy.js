describe.skip("edit patches on property page", { tags: ["@property", "@authentication", "@common", "@root"] }, () => {
    it("patch assignment editing was removed from the property view sidebar", () => {
        // Patch edit UI (edit-assignment-button, patch-dropdown-options) is no longer
        // shown on the property view. New asset patch assignment is covered separately.
    });
});
