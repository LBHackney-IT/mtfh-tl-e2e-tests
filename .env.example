# Optional local overrides
# Cognito credentials and API endpoints are loaded from SSM by setEnv.sh.
#
#   source setEnv.sh <aws-sso-profile> development
#
# Uncomment only if you need to override defaults:

# export CYPRESS_COGNITO_FLOW_ENABLED_FOR="development"

# Legacy JWT auth — only when Cognito flow is disabled for your environment
# export CYPRESS_E2E_ACCESS_TOKEN_DEVELOPMENT='<<your hackney JWT>>'
# export CYPRESS_E2E_ACCESS_TOKEN_STAGING='<<staging JWT>>'
# export CYPRESS_E2E_ACCESS_TOKEN_PRODUCTION='<<production JWT>>'
